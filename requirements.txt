# 버전 고정 — 2026-08-04 기준, 로컬에서 실제 생성 1회를 통과시킨 조합입니다.
#
# 고정 이전에는 11개 패키지가 전부 버전 없이 나열되어 있어 CI 가 매번 최신을
# 설치했습니다. google-genai 나 pydantic 의 breaking change 하나로 발행이 멈출 수
# 있었고, 실패해도 같은 환경을 재현할 수 없었습니다.
#
# 갱신 방법: 로컬에서 버전을 올린 뒤 실제 생성 1회와
# `python scripts/validate_content.py` 를 통과시키고 나서 이 파일을 고치세요.

google-genai==1.70.0
google-api-core==2.25.2
python-dotenv==1.2.1
tenacity==9.1.4
requests==2.32.5
beautifulsoup4==4.14.3
pydantic==2.12.5
playwright==1.58.0
Pillow==11.3.0
feedparser==6.0.12
PyYAML==6.0.3
